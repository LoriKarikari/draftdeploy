services:
  web:
    image: mcr.microsoft.com/azuredocs/aci-helloworld:latest
    ports:
      - "80:80"

  api:
    image: ealen/echo-server:latest
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
